package com.restassureddemo;

public class Theory_POJO {
	/*
	 * there will be a communication between client & server.so for e.g. when 
	 * client to server communication is in  process what we do, we create a java 
	 * class i.e. USer_DTO where we concert java object to jason to send the request 
	 * so at server side there is also USER DTO , where data is getting map 
	 *  i.e. JSON to java object & vice versa . its nothing but serialization
	 *  & Deserialization
	 *  
	 *  Serialization :- Object to JSON
	 *  DeSerialization:- JSON to Object/POJO
	 *  
	 *  POJO:- Plain old java object
	 *  
	 *  
	 *  
	 *  
	 * 
	 */

}
